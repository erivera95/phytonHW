# Crea una clase base `ReporteVentas` con un método `generar_reporte()`.
# Luego, crea dos clases hijas:
# - `ReporteMensual`, que genera un reporte con las ventas de un mes.
# - `ReporteAnual`, que genera un reporte con las ventas del año.
# Implementa `generar_reporte()` de forma diferente en cada clase usando ciclos.
